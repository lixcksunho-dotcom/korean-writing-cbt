@AGENTS.md

# 실글패스(kptest) 작업 규칙

실제 결제가 일어나는 운영 서비스다. 돈·DB·배포에 닿는 변경은 사람이 판단한다.

## 이 프로젝트에서 지켜야 할 것

- 매출 원장은 **포트원**이다. `subscriptions` 테이블에는 무료 발급이 섞여 있어 매출이 아니다(무료는 `amount=0`).
- 유료 API를 새로 호출하지 않는다. 조회는 무료 범위에서만.
- `.env.local` 의 키는 읽되 로그·리포트·커밋에 값을 남기지 않는다.
- 검증용 시드는 `deleteMany({})` 로 지우지 않는다. 만든 것만 스코프 삭제.

## 자율 개발 루프 규칙

### 워커 모드 ("워커 규칙에 따라 진행해"라는 지시를 받았을 때)
1. NEED_HUMAN.md가 존재하면: 아무 작업도 하지 말고 "사람 확인 대기 중"만 출력하고 종료
2. REVIEW.md가 존재하면 그 수정 지시를 처리한다. 완료 신호를 확인한 실행기가 REVIEW.md를 삭제하고 커밋에 포함한다.
3. 둘 다 없으면: BACKLOG.md의 첫 번째 미완료 항목([ ]) 하나만 진행. **⏸️ 로 시작하는 항목**은 조건이 안 맞으면 건너뛰고 다음 항목으로
4. 브랜치 생성·전환과 커밋은 바깥 실행기가 한다. Codex는 Git 쓰기를 하지 않고 준비된 work 브랜치에서 파일을 바로 저장한다.
5. 오프라인 검사만 실행하고 REPORT.md에 작업 요약·변경 파일·테스트 방법과 실제 실행 결과를 기록한다. 마지막 메시지에 '커밋 메시지 제안: ...'을 적고 마지막 줄은 WORKER_DONE으로 끝낸다. 미완료이면 실행기가 wip 커밋 후 한 번만 이어서 실행한다.
6. **실행 가능한 항목이 없으면 제안 모드**: `PROPOSALS.md` 에 2~3건을 ①무엇을 ②왜(기존 로그·REPORT·git 이력 근거) ③범위 제한 형식으로 적는다. 코드 변경·브랜치 생성 금지. 미처리 제안이 3건 이상이면 "제안 대기 중"만 출력하고 종료. 제안을 스스로 BACKLOG로 옮기지 말 것
7. 지시문의 숫자·파일명·전제는 **틀릴 수 있다.** 시작 전에 실물로 대조하고, 다르면 REPORT에 "지시문과 달랐던 점"을 적은 뒤 실물 기준으로 진행

### 리뷰어 모드 ("리뷰어 규칙에 따라 검수해"라는 지시를 받았을 때)
1. NEED_HUMAN.md가 있으면 종료. work/ 브랜치가 없으면 "검수할 작업 없음"만 출력하고 종료
2. 가장 최근 work/ 브랜치의 diff와 REPORT.md를 검토: 요구사항 충족·기존 기능 무파괴·**REPORT에 적힌 테스트가 실제로 수행됐는지**
3. 통과: 마지막 메시지 첫 줄에 PASS. 실행기가 main 병합·BACKLOG 완료 표시·REPORT 추가·커밋·브랜치 삭제를 한다.
4. 반려: 마지막 메시지 첫 줄에 FAIL, 아래에 REVIEW.md에 들어갈 [무엇이 / 왜 문제이고 / 어떻게 고칠지]를 쓴다. 파일 저장은 실행기가 한다.
5. 실행기가 반려 이력을 세고 같은 브랜치의 3회째 반려에 NEED_HUMAN.md를 만든다. 병합 충돌도 실행기가 abort하고 반려 문서에 기록한다.
6. Codex 리뷰어는 오프라인 검토와 판정만 한다. 코드·문서 수정 및 모든 Git 쓰기를 금지한다.

### 워커가 반드시 NEED_HUMAN.md 로 넘겨야 하는 것

돈이 걸린 경로는 되돌리기가 어렵거나 불가능하다. 아래는 **조사·수정안 문서화까지만** 하고 멈춘다.

- **결제 코드 변경**: `src/app/api/portone/**`, 결제창 호출부, 금액·상품 정의
- **환불·취소 실행**, 포트원 API의 파괴적 호출(무료 조회는 허용)
- **DB 스키마 변경**과 마이그레이션 실행(`db push`, `migrate`)
- **프로덕션 배포**(`vercel --prod`), 환경변수 변경
- 실제 결제를 발생시키는 테스트 — 모의(mock)로만 검증할 것
- 사용자 데이터(구독·결제·회원) 삭제·일괄 수정
