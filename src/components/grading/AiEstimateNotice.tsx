// AI가 매긴 점수는 참고값이라는 안내.
//
// 이용약관이 "AI 채점·첨삭 결과는 학습 보조를 위한 참고 정보이며, 실제 시험의 채점
// 결과와 다를 수 있습니다"라고 적어 두었는데, 정작 점수가 나오는 화면에는 그 말이
// 없었다. 화면에는 "18 / 100점 (18%)"만 크게 뜬다 — 약관을 읽고 들어오는 사람은
// 없으므로, 실제로는 아무도 안내받지 못한 것과 같다.
//
// 문구를 한 곳에 둔다. 화면마다 따로 적으면 한쪽만 고쳐져 서로 다른 말을 하게 된다.

export default function AiEstimateNotice({ className = '' }: { className?: string }) {
  return (
    <p className={`text-xs text-[#64748b] ${className}`}>
      AI가 매긴 참고 점수예요. 실제 시험의 채점 결과와 다를 수 있습니다.
    </p>
  )
}
