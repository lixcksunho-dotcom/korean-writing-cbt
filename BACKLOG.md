# 작업 백로그 (위에서부터 순서대로 처리)

<!-- 규칙: 한 줄에 작업 하나, 구체적으로. 완료되면 리뷰어가 [x]로 바꿈 -->
<!-- 우선순위는 오직 '순서'로만 표현한다. 급한 건 위로 옮길 것 -->
<!-- 결제·DB·배포에 닿는 항목은 CLAUDE.md 규칙대로 조사·수정안 문서화까지만 -->

- [x] 빈 시험 세션 정리 방안 `docs/empty_session_plan.md`: `/cbt/[회차]` 를 여는 순간 `getOrCreateExamSession` 이 세션 행을 만든다 — 그래서 미완료 233건 중 112건이 '눌러 보고 나간' 빈 행이고, 완주율이 49%(열어 본 것 포함)와 95%(한 문제라도 푼 것) 사이에서 두 배로 갈린다(2026-09-08 `npm run check:dropoff` 실측). ①세션을 '시작' 시점에 만들려면 어디를 고쳐야 하는지 ②같은 회차 미완료 중복(코드 주석에 이미 적힌 0~3초 간격 중복)을 막을 부분 유니크 인덱스 초안 ③이미 쌓인 빈 행을 어떻게 다룰지(지우지 말고 기준만) 를 문서로 남긴다. 왜: 지금은 어떤 숫자를 봐도 '들어와서 안 푸는 사람'과 '풀다 만 사람'이 섞여 있어 다음 개선을 어디에 걸지 정할 수 없다. 범위: **문서와 SQL 초안까지만** — 마이그레이션 실행·행 삭제·화면 코드 수정 금지(DB 변경은 사람이 SQL Editor 에서 돌린다)
- [x] 시험 화면 첫 30초 관찰 `scripts/exam_entry_check.mjs`: 로그인한 검사 계정으로 `/cbt/<무료 회차>` 를 열어 ①첫 화면에 무엇이 보이는지(문항이 바로 뜨는지, 안내·타이머가 먼저인지) ②'저장하고 나가기'가 눈에 띄는 자리에 있는지 ③좁은 화면(390px)에서 첫 문항이 접히지 않는지 를 캡처와 함께 REPORT 에 남긴다. 왜: 열고 한 문제도 안 푼 회차가 112건이다 — 첫 화면에서 무엇이 사람을 돌려세우는지 아무도 본 적이 없다. 범위: **읽기·캡처만**, 답안 제출 금지, 만든 세션은 검사 끝에 지울 것
- [ ] 간편결제 잔여 경로 점검 `scripts/payment_method_diagnose.mjs` 재실행 후 표 갱신: 카카오페이가 실제로 쓰이고 있으니(후 구간 결제 6건) 네이버페이·토스 등 나머지 간편결제가 지금 어떤 상태인지 다시 확인한다. ①현재 열려 있는 payMethod ②easyPayProvider 지정 ③각 수단을 열려면 무엇이 더 필요한지(채널 추가 심사 여부 포함)를 REPORT 에 표로 남긴다. 왜: 결제수단 하나를 늘렸더니 완결이 5건→10건이 됐다. 다음 수단의 비용 대비 효과를 판단할 근거가 필요하다. 범위: **읽기와 문서화만 — 결제 코드 수정·실결제 테스트 절대 금지**(NEED_HUMAN 대상). 심사·계약이 필요한 항목은 그 사실만 적을 것
- [x] ⏸️ **KST 00:00~09:00 사이에만** 라이브 화면 검사로 시간대 수정 최종 확인 — 조사·보고만: `npm run check:pages`(기본 대상 kptest.cloud) 를 돌려 로그인 화면들(`/dashboard` `/cbt` `/practice` 계열)에 React #418(hydration 불일치)이 **없는지** 확인하고 결과 전문·실행 시각(KST)을 REPORT 에 기록. 왜: 제121회 접수 마감이 9/7 이라 9/8 새벽에 서버(UTC, 아직 9/7 "접수 중 D-0")와 브라우저(KST 9/8 "접수 마감")가 어긋날 예정이었고, 시험일정 '오늘'을 한국 날짜 기준으로 고쳤다(REPORT 9/7 절). 로컬에선 재현이 안 돼 라이브 새벽 검사만이 최종 증거다. 09:00 을 넘겼으면 건너뛴다(⏸️ 규칙). 범위: 검사 실행·기록만, 코드 무변경. 오류가 있으면 콘솔 메시지 전문과 화면 목록만 REPORT 에 적을 것
- [x] 관리자 회원·결제 화면 페이지네이션 `src/app/admin/(protected)/members/page.tsx`·`payments/page.tsx`: `listUsers({ perPage: 1000 })` 과 `subscriptions .limit(1000)` 을 페이지 단위(100건, 이전/다음)로 바꾼다. 왜: PostgREST·GoTrue 는 1000행에서 조용히 자른다 — 지금 회원 135명·이용권 30건이라 안전하지만 넘는 순간 화면 숫자가 틀린 채 경고가 없다(`npm run check:row-cap` 이 80% 에서 경고하도록 9/7 추가). 범위: 두 화면과 그 하위 컴포넌트만, 집계 숫자(총원·매출 합계)는 `count: 'exact'`/서버 집계로 전체 기준을 유지할 것(페이지만 세면 숫자가 줄어든다), `scripts/current_members.mjs` 의 per_page=1000 도 같은 방식으로. 확인은 `check:row-cap` 통과 + `check:admin` 통과 + 페이지 넘김을 실제로 눌러 본 결과를 REPORT 에
- [x] 결제 퍼널 일일 요약에 회원 필터 붙이기 `scripts/payment_funnel_daily.mjs` (2026-09-07 관리 틱, `docs/checkout_dropoff_plan.md` 1순위 제안): `payment_attempt_report.mjs:40-45` 가 이미 하는 "지금 회원인 사람의 시도만 센다"(auth admin users 대조, 탈퇴·검증 계정 제외 건수 출력)를 일일 요약에도 똑같이 적용한다 — 지금은 `grep member|listUsers` 0건으로 필터가 없다. 왜: 8/23 "진입 2건 → 시도 0건"은 실사용자 이탈이 아니라 검증 스크립트 발자국(회원 목록에 없는 두 계정)이었다(REPORT 8/28 실측). 필터 없이는 매일 아침 요약이 가짜 이탈을 보고한다. 범위: 스크립트 한 파일, **조회 전용**(포트원·Supabase 읽기만), 필터 로직은 복붙 말고 `payment_attempt_report.mjs` 에서 함수로 빼 둘이 같이 쓰게 할 것, 개인정보는 id 앞 8자만. 확인은 `--date 2026-08-23` 재실행 결과가 "진입 0건(검증 계정 2건 제외)" 로 바뀌는지와 최근 날짜 결과가 종전과 같은지 둘 다 REPORT 에 기록
- [x] 1000행 조용한 상한 대비 검사 `scripts/row_cap_check.mjs` + `npm run check:row-cap`: 관리자 화면 4곳이 상한을 박아 두었다 — `src/app/admin/(protected)/page.tsx:24` `.limit(1000)` · `payments/page.tsx:15` `.limit(1000)` · `members/page.tsx:15`·`payments/page.tsx:86` `listUsers perPage 1000`. 각 대상 테이블·회원 수를 `count: 'exact'`(또는 auth admin 총원)로 실제로 세어 상한 대비 몇 %인지 출력하고, 80% 이상이면 경고·exit 1. 덤으로 `src/` 전체에서 `.range(` 없이 전량을 읽는 select 중 1000행을 넘을 수 있는 테이블(문항·`page_views`·답안)을 파일:줄로 나열만 한다(수정 금지). 왜: PostgREST 는 limit 을 무시하고 1000행에서 조용히 자른다 — 이 저장소 30일 방문 통계가 그래서 며칠치만 나왔었다(커밋 b80e086). 회원 89명이라 지금은 안전하지만, 넘는 순간 화면 숫자가 틀린 채로 아무 경고가 없다. 범위: **읽기 전용** 검사 스크립트 신규 + package.json 1줄, 화면 코드 무변경, 페이지네이션 도입은 결과 보고 후 별도 항목으로
- [x] 카카오페이 7일 대 7일 비교 실행 `npm run report:method-impact` 재실행 후 결론 기록: 8/28 작성 시점에는 "후"가 1일치뿐이라 비교가 불가능했다. 9/3 이 지나 이제 전 7일(8/20~8/26) 대 후 7일(8/27~9/2)이 채워졌으니 실제로 돌려 진입·시도·완결을 대조하고 REPORT.md 에 결론을 적는다. 왜: 카카오페이를 붙인 것이 전환에 효과가 있었는지 아직 아무도 모르고, 이걸 모르면 다음 개선을 결제수단에 걸지 유입에 걸지 정할 수 없다. 범위: **스크립트 무수정·조회 전용** — 표본이 여전히 한 자릿수면 "효과 있음/없음"으로 단정하지 말고 숫자와 한계를 함께 적을 것, 새 스케줄·알림 금지
- [x] 유입 경로별 결제 전환 `scripts/inflow_to_payment.mjs`: 최근 30일 결제 시도자의 유입 경로(가입 시 기록된 referrer·utm 이 있으면 그것, 없으면 "미상")를 세어 경로별 진입·완결 건수를 표로 출력. 왜: 결제창 완결률(9건 중 5건)은 나쁘지 않은데 결제창에 오는 사람이 하루 한두 명이다 — 병목이 결제수단이 아니라 유입 쪽인지 숫자로 확인해야 다음 작업을 어디에 걸지 정해진다. 범위: **읽기 전용** — 개인정보는 id 앞 8자만, 추적 코드·외부 분석도구 추가 금지, 기록된 유입 정보가 없으면 "수집 안 되고 있음"을 명시하고 무엇을 남겨야 하는지만 REPORT에 적을 것(구현은 하지 말 것)
- [x] 무료 체험에서 유료로 넘어간 비율 `scripts/free_to_paid.mjs`: `subscriptions` 에서 무료 발급(amount=0)을 받은 사람 중 이후 유료 결제까지 간 사람 수와 그 사이 걸린 일수를 집계. 왜: 무료 발급이 매출로 이어지는지 한 번도 측정된 적이 없다. 이어지지 않으면 무료 회차 수 자체가 재검토 대상이 된다. 범위: **읽기 전용** — 데이터 수정 금지, 개인정보는 id 앞 8자만, 표본이 10명 미만이면 비율(%) 대신 실제 인원수로만 표기할 것(작은 표본에 비율을 쓰면 과대해석된다)
- [x] 8/27 결제 1건이 순매출 0원인 경위 조사 `scripts/same_day_refund_trace.mjs`: 포트원 원장에서 2026-08-27 결제 건의 `status`·`cancellations`·취소 시각·사유를 뽑아 "당일 결제 → 당일 취소"인지, 아니면 결제 실패가 완결로 잘못 집계된 것인지 판정해 REPORT.md 에 기록. 왜: `daily_revenue.mjs` 가 8/27 을 "0원 1건"으로 내는데, 결제가 잡혔는데 매출이 0이면 ①즉시 환불 ②부분취소 ③집계 버그 셋 중 하나이고 어느 쪽이냐에 따라 대응이 완전히 다르다. 범위: **조회 전용** — 취소·환불 실행 절대 금지, 고객 식별정보는 id 앞 8자만 출력, 판정이 애매하면 단정하지 말고 가능성과 근거를 나란히 적을 것
- [x] 카카오페이 도입 전후 전환 비교 `scripts/payment_method_impact.mjs`: 카카오페이 연동 커밋(2026-08-24) 을 기준으로 그 전 7일과 후 7일의 결제창 진입·시도·완결을 건/사람 단위로 나란히 출력. 왜: 8/24 직후 3건 16,500원이 잡혔고 주간 매출이 11,000원→22,000원으로 늘었는데, 이게 결제수단 확대 효과인지 우연인지 구분되지 않은 채로는 다음 개선을 어디에 걸지 정할 수 없다. 범위: 기존 `summarizeAttempts` 재사용·조회 전용 — 표본이 작다는 사실(양쪽 모두 한 자릿수)을 출력에 반드시 병기하고 "효과 있음"으로 단정하지 말 것, 새 스케줄·알림 금지
- [x] 결제창 진입 후 이탈 계측 설계 `docs/checkout_dropoff_plan.md`: 8/23 처럼 "진입 2건 → 시도 0건"인 날이 왜 생기는지 알아내려면 무엇을 계측해야 하는지 설계만 문서로 남긴다. ①현재 어느 지점까지 로그가 남는지 실물 확인 ②비어 있는 구간 ③추가할 이벤트 후보와 각각의 구현 난이도·개인정보 영향. 왜: 진입 대비 시도 0건이 반복되면 결제수단을 늘려도 매출이 안 늘고, 지금은 이탈 지점을 볼 수단 자체가 없다. 범위: **설계 문서만** — 코드·추적 스크립트 추가 금지, 외부 분석 도구 도입 제안 금지(비용 발생), 개인정보를 남기는 방식은 후보에서 제외할 것
- [x] 간편결제 경로 진단 리포트 `scripts/payment_method_diagnose.mjs`: 결제창 호출부(`src/` 내 PortOne SDK `requestPayment` 호출 지점)를 읽어 ①현재 열려 있는 `payMethod` 값 ②`easyPayProvider` 지정 여부 ③이니시스 V2 규격상 카카오페이/네이버페이가 뜨려면 무엇이 더 필요한지를 표로 정리해 `REPORT.md` 에 기록. 왜: 결제 수단이 카드만 열려 있고 간편결제는 "눌리는데 안 되는" 형태로 실패해 화면에 오류가 안 나타난다(사람 실측). 최근 7일 매출 11,000원·결제 2건이라 전환 손실 규모가 크다. 범위: **읽기와 문서화만 — 결제 코드 수정·실제 결제 테스트 절대 금지**(CLAUDE.md NEED_HUMAN 대상). 수정안은 REPORT에 diff 예시로만 제시하고 적용하지 말 것
- [x] 결제 퍼널 일일 요약 `scripts/payment_funnel_daily.mjs`: 기존 `payment_attempt_report.mjs` 의 `summarizeAttempts` 를 재사용해 **어제 하루** 기준 결제창 진입·시도·완결을 건/사람 단위로 뽑고 한 줄 요약을 stdout 에 출력. 왜: 현재 퍼널 리포트는 기간 전체 누적이라 "어제 뭐가 달라졌는지"를 못 본다. 범위: 포트원 조회 API만 사용(무료 범위) — 새 테이블·스케줄 등록·알림 전송 금지, 결제 0건인 날에도 정상 종료(0건임을 명시 출력), 기존 리포트 스크립트 무수정
- [x] 무료 발급과 유료 결제 분리 검증 `scripts/revenue_integrity_check.mjs`: `subscriptions` 테이블 행 중 `amount=0`(무료 발급)과 유료 결제 건수를 세고, 같은 기간 포트원 원장 결제 건수와 대조해 불일치가 있으면 목록으로 출력. 왜: 매출 집계에서 무료 발급이 섞이면 순익이 과대 계상된다(원장은 포트원). 범위: **읽기 전용** — 데이터 수정·삭제 금지, 개인정보(이메일 등)는 출력하지 말고 id 앞 8자만, 불일치가 0건이면 "일치"만 출력
